cmake_minimum_required(VERSION 3.0.2)
project(iiwa_needle_description)

find_package(catkin REQUIRED)

catkin_package(
)

## TODO: Install the URD, meshes, and launch files.
# install(FILES
#   # myfile1
#   # myfile2
#   DESTINATION ${CATKIN_PACKAGE_SHARE_DESTINATION}
# )

